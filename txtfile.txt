hello helvatica

git init initialise git 
git status shows status
git add 
git commit commit the changes
git diff gives the difference bw previous commit and now 
git log shows all logs 
git checkout "id of log " gives that log details 

branching in git ...

git branch used to check all branch 

git checkout -b "name of branch without quote "

git checkout branch to change branch
git merge "branch name " to merge with master 

git delete -d "branch name " to delete branch 





github.com

create new repo follow instruction 
git push origin "branch"

origin rfer to main server on github
master is your local one
